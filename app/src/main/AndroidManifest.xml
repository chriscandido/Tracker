<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.envisage.tracker">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Activities -->
        <activity
            android:name="com.envisage.tracker.activity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.envisage.tracker.activity.InformationActivity"/>
        <activity android:name="com.envisage.tracker.activity.SliderAdapterActivity" />
        <activity android:name="com.envisage.tracker.activity.EulaActivity" />
        <activity android:name="com.envisage.tracker.activity.LoginActivity" />
        <activity android:name="com.envisage.tracker.MainActivity" />
        <activity android:name="com.envisage.tracker.DashboardActivity" />
        <activity android:name="com.envisage.tracker.activity.UpdateSymptomsActivity" />
        <activity android:name="com.envisage.tracker.activity.MapActivity" />
        <activity android:name="com.envisage.tracker.activity.StaySafeActivity" />
        <activity android:name="com.envisage.tracker.activity.ContactTracingActivity" />
        <activity
            android:name="com.envisage.tracker.utils.GoogleMaps"
            android:label="Google Maps">

        </activity>
        <!-- Service -->
        <service
            android:name="com.envisage.tracker.service.LocationService"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name="com.envisage.tracker.service.ApolloService"
            android:enabled="true"
            android:exported="false" />
        <!-- Receiver -->
        <receiver
            android:name="com.envisage.tracker.activity.BootBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- meta data -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" /> <!-- Google API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>